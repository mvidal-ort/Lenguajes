{-# LANGUAGE CPP #-}
#if __GLASGOW_HASKELL__ <= 708
{-# LANGUAGE OverlappingInstances #-}
#endif
{-# LANGUAGE FlexibleInstances #-}
{-# OPTIONS_GHC -fno-warn-incomplete-patterns #-}

-- | Pretty-printer for PrintCPP.
--   Generated by the BNF converter.

module PrintCPP where

import qualified AbsCPP
import Data.Char

-- | The top-level printing method.

printTree :: Print a => a -> String
printTree = render . prt 0

type Doc = [ShowS] -> [ShowS]

doc :: ShowS -> Doc
doc = (:)

render :: Doc -> String
render d = rend 0 (map ($ "") $ d []) "" where
  rend i ss = case ss of
    "["      :ts -> showChar '[' . rend i ts
    "("      :ts -> showChar '(' . rend i ts
    "{"      :ts -> showChar '{' . new (i+1) . rend (i+1) ts
    "}" : ";":ts -> new (i-1) . space "}" . showChar ';' . new (i-1) . rend (i-1) ts
    "}"      :ts -> new (i-1) . showChar '}' . new (i-1) . rend (i-1) ts
    ";"      :ts -> showChar ';' . new i . rend i ts
    t  : ts@(p:_) | closingOrPunctuation p -> showString t . rend i ts
    t        :ts -> space t . rend i ts
    _            -> id
  new i   = showChar '\n' . replicateS (2*i) (showChar ' ') . dropWhile isSpace
  space t = showString t . (\s -> if null s then "" else ' ':s)

  closingOrPunctuation :: String -> Bool
  closingOrPunctuation [c] = c `elem` closerOrPunct
  closingOrPunctuation _   = False

  closerOrPunct :: String
  closerOrPunct = ")],;"

parenth :: Doc -> Doc
parenth ss = doc (showChar '(') . ss . doc (showChar ')')

concatS :: [ShowS] -> ShowS
concatS = foldr (.) id

concatD :: [Doc] -> Doc
concatD = foldr (.) id

replicateS :: Int -> ShowS -> ShowS
replicateS n f = concatS (replicate n f)

-- | The printer class does the job.

class Print a where
  prt :: Int -> a -> Doc
  prtList :: Int -> [a] -> Doc
  prtList i = concatD . map (prt i)

instance {-# OVERLAPPABLE #-} Print a => Print [a] where
  prt = prtList

instance Print Char where
  prt _ s = doc (showChar '\'' . mkEsc '\'' s . showChar '\'')
  prtList _ s = doc (showChar '"' . concatS (map (mkEsc '"') s) . showChar '"')

mkEsc :: Char -> Char -> ShowS
mkEsc q s = case s of
  _ | s == q -> showChar '\\' . showChar s
  '\\'-> showString "\\\\"
  '\n' -> showString "\\n"
  '\t' -> showString "\\t"
  _ -> showChar s

prPrec :: Int -> Int -> Doc -> Doc
prPrec i j = if j < i then parenth else id

instance Print Integer where
  prt _ x = doc (shows x)

instance Print Double where
  prt _ x = doc (shows x)

instance Print AbsCPP.Id where
  prt _ (AbsCPP.Id (_,i)) = doc (showString i)
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print AbsCPP.Program where
  prt i e = case e of
    AbsCPP.PDefs defs -> prPrec i 0 (concatD [prt 0 defs])

instance Print AbsCPP.Def where
  prt i e = case e of
    AbsCPP.DUsing idqs -> prPrec i 0 (concatD [doc (showString "using"), prt 0 idqs, doc (showString ";")])
    AbsCPP.DFun type_ id args stms -> prPrec i 0 (concatD [prt 0 type_, prt 0 id, doc (showString "("), prt 0 args, doc (showString ")"), doc (showString "{"), prt 0 stms, doc (showString "}")])
    AbsCPP.DFunInline type_ id args stms -> prPrec i 0 (concatD [doc (showString "inline"), prt 0 type_, prt 0 id, doc (showString "("), prt 0 args, doc (showString ")"), doc (showString "{"), prt 0 stms, doc (showString "}")])
  prtList _ [] = concatD []
  prtList _ (x:xs) = concatD [prt 0 x, prt 0 xs]

instance Print [AbsCPP.Def] where
  prt = prtList

instance Print AbsCPP.Arg where
  prt i e = case e of
    AbsCPP.ADecl type_ id -> prPrec i 0 (concatD [prt 0 type_, prt 0 id])
    AbsCPP.ADecl2 type_ ids -> prPrec i 0 (concatD [doc (showString "const"), doc (showString "vector"), doc (showString "<"), prt 0 type_, doc (showString ">"), prt 0 ids])
    AbsCPP.ADecl3 type_ ids -> prPrec i 0 (concatD [doc (showString "const"), doc (showString "vector"), doc (showString "<"), prt 0 type_, doc (showString ">"), doc (showString "&"), prt 0 ids])
    AbsCPP.ADecl4 type_ id -> prPrec i 0 (concatD [doc (showString "const"), prt 0 type_, prt 0 id])
    AbsCPP.ADecl5 type_ id -> prPrec i 0 (concatD [doc (showString "const"), prt 0 type_, doc (showString "&"), prt 0 id])
    AbsCPP.ADecl6 id ids -> prPrec i 0 (concatD [doc (showString "const"), prt 0 id, doc (showString "&"), prt 0 ids])
    AbsCPP.ADecl7 type_ restarg -> prPrec i 0 (concatD [doc (showString "const"), prt 0 type_, doc (showString "["), doc (showString "]"), prt 0 restarg])
  prtList _ [] = concatD []
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print AbsCPP.RestArg where
  prt i e = case e of
    AbsCPP.RestArg1 id -> prPrec i 0 (concatD [prt 0 id])
    AbsCPP.RestArg2 ids -> prPrec i 0 (concatD [doc (showString "&"), prt 0 ids])

instance Print [AbsCPP.Arg] where
  prt = prtList

instance Print AbsCPP.Stm where
  prt i e = case e of
    AbsCPP.SExp exp -> prPrec i 0 (concatD [prt 0 exp, doc (showString ";")])
    AbsCPP.SDecls type_ ids -> prPrec i 0 (concatD [prt 0 type_, prt 0 ids, doc (showString ";")])
    AbsCPP.SDeclsArray type_ ids exp -> prPrec i 0 (concatD [prt 0 type_, prt 0 ids, doc (showString "["), prt 0 exp, doc (showString "]"), doc (showString ";")])
    AbsCPP.SInit type_ id exp -> prPrec i 0 (concatD [prt 0 type_, prt 0 id, doc (showString "="), prt 0 exp, doc (showString ";")])
    AbsCPP.SConst type_ id -> prPrec i 0 (concatD [doc (showString "const"), prt 0 type_, prt 0 id, doc (showString ";")])
    AbsCPP.SConstPointer type_ id -> prPrec i 0 (concatD [doc (showString "const"), prt 0 type_, doc (showString "&"), prt 0 id, doc (showString ";")])
    AbsCPP.SConstVector id type_ ids -> prPrec i 0 (concatD [doc (showString "const"), prt 0 id, doc (showString "<"), prt 0 type_, doc (showString ">"), prt 0 ids, doc (showString ";")])
    AbsCPP.SConstVectorPointer id type_ ids -> prPrec i 0 (concatD [doc (showString "const"), prt 0 id, doc (showString "<"), prt 0 type_, doc (showString ">"), doc (showString "&"), prt 0 ids, doc (showString ";")])
    AbsCPP.SReturn exp -> prPrec i 0 (concatD [doc (showString "return"), prt 0 exp, doc (showString ";")])
    AbsCPP.SReturnVoid -> prPrec i 0 (concatD [doc (showString "return"), doc (showString ";")])
    AbsCPP.SWhile exp stm -> prPrec i 0 (concatD [doc (showString "while"), doc (showString "("), prt 0 exp, doc (showString ")"), prt 0 stm])
    AbsCPP.SDo exp1 exp2 -> prPrec i 0 (concatD [doc (showString "do"), prt 0 exp1, doc (showString ";"), doc (showString "while"), doc (showString "("), prt 0 exp2, doc (showString ")")])
    AbsCPP.SFor type_ id exp1 exp2 exp3 stm -> prPrec i 0 (concatD [doc (showString "for"), doc (showString "("), prt 0 type_, prt 0 id, doc (showString "="), prt 0 exp1, doc (showString ";"), prt 8 exp2, doc (showString ";"), prt 11 exp3, doc (showString ")"), prt 0 stm])
    AbsCPP.SBlock stms -> prPrec i 0 (concatD [doc (showString "{"), prt 0 stms, doc (showString "}")])
    AbsCPP.SIf exp stm restif -> prPrec i 0 (concatD [doc (showString "if"), doc (showString "("), prt 0 exp, doc (showString ")"), prt 0 stm, prt 0 restif])
    AbsCPP.STypedef restotypedef -> prPrec i 0 (concatD [doc (showString "typedef"), prt 0 restotypedef, doc (showString ";")])
    AbsCPP.SStruct id reststruct -> prPrec i 0 (concatD [doc (showString "struct"), prt 0 id, doc (showString "{"), prt 0 reststruct, doc (showString "}"), doc (showString ";")])
  prtList _ [] = concatD []
  prtList _ (x:xs) = concatD [prt 0 x, prt 0 xs]

instance Print AbsCPP.RestIf where
  prt i e = case e of
    AbsCPP.SIfElse stm -> prPrec i 0 (concatD [doc (showString "else"), prt 0 stm])
    AbsCPP.SIFSimple -> prPrec i 0 (concatD [])

instance Print AbsCPP.RestoTypedef where
  prt i e = case e of
    AbsCPP.STypedef2 exp -> prPrec i 0 (concatD [prt 15 exp])

instance Print AbsCPP.RestStruct where
  prt i e = case e of
    AbsCPP.SStructArg type_ id -> prPrec i 0 (concatD [prt 0 type_, prt 0 id, doc (showString ";")])
    AbsCPP.SStructArgVector id type_ -> prPrec i 0 (concatD [prt 0 id, doc (showString "<"), prt 0 type_, doc (showString ">"), doc (showString ";")])

instance Print [AbsCPP.Stm] where
  prt = prtList

instance Print AbsCPP.Exp where
  prt i e = case e of
    AbsCPP.ETrue -> prPrec i 16 (concatD [doc (showString "true")])
    AbsCPP.EFalse -> prPrec i 16 (concatD [doc (showString "false")])
    AbsCPP.EInt n -> prPrec i 16 (concatD [prt 0 n])
    AbsCPP.EDouble d -> prPrec i 16 (concatD [prt 0 d])
    AbsCPP.EStrings strs -> prPrec i 16 (concatD [prt 0 strs])
    AbsCPP.EChar c -> prPrec i 16 (concatD [prt 0 c])
    AbsCPP.EIndexing1 id exp -> prPrec i 15 (concatD [prt 0 id, doc (showString "["), prt 11 exp, doc (showString "]")])
    AbsCPP.EIds idqs -> prPrec i 15 (concatD [prt 0 idqs])
    AbsCPP.EIndexing3 idqs id -> prPrec i 15 (concatD [prt 0 idqs, prt 0 id])
    AbsCPP.EApp id exps -> prPrec i 15 (concatD [prt 0 id, doc (showString "("), prt 0 exps, doc (showString ")")])
    AbsCPP.EPoint exp1 exp2 -> prPrec i 14 (concatD [prt 14 exp1, doc (showString "."), prt 15 exp2])
    AbsCPP.EPointer exp1 exp2 -> prPrec i 14 (concatD [prt 14 exp1, doc (showString "->"), prt 15 exp2])
    AbsCPP.EPIncr exp -> prPrec i 14 (concatD [prt 15 exp, doc (showString "++")])
    AbsCPP.EPDecr exp -> prPrec i 14 (concatD [prt 15 exp, doc (showString "--")])
    AbsCPP.EExpo exp -> prPrec i 14 (concatD [doc (showString "*"), prt 15 exp])
    AbsCPP.EIncr exp -> prPrec i 13 (concatD [doc (showString "++"), prt 14 exp])
    AbsCPP.EDecr exp -> prPrec i 13 (concatD [doc (showString "--"), prt 14 exp])
    AbsCPP.EPot exp -> prPrec i 13 (concatD [doc (showString "!"), prt 14 exp])
    AbsCPP.ETimes exp1 exp2 -> prPrec i 12 (concatD [prt 12 exp1, doc (showString "*"), prt 13 exp2])
    AbsCPP.EDiv exp1 exp2 -> prPrec i 12 (concatD [prt 12 exp1, doc (showString "/"), prt 13 exp2])
    AbsCPP.EPorcent exp1 exp2 -> prPrec i 12 (concatD [prt 12 exp1, doc (showString "%"), prt 13 exp2])
    AbsCPP.EPlus exp1 exp2 -> prPrec i 11 (concatD [prt 11 exp1, doc (showString "+"), prt 12 exp2])
    AbsCPP.EMinus exp1 exp2 -> prPrec i 11 (concatD [prt 11 exp1, doc (showString "-"), prt 12 exp2])
    AbsCPP.ELeftShift exp1 exp2 -> prPrec i 10 (concatD [prt 10 exp1, doc (showString "<<"), prt 11 exp2])
    AbsCPP.ERightShift exp1 exp2 -> prPrec i 10 (concatD [prt 10 exp1, doc (showString ">>"), prt 11 exp2])
    AbsCPP.ELt exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString "<"), prt 10 exp2])
    AbsCPP.EGt exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString ">"), prt 10 exp2])
    AbsCPP.ELtEq exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString "<="), prt 10 exp2])
    AbsCPP.EGtEq exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString ">="), prt 10 exp2])
    AbsCPP.EEq exp1 exp2 -> prPrec i 8 (concatD [prt 8 exp1, doc (showString "=="), prt 9 exp2])
    AbsCPP.ENEq exp1 exp2 -> prPrec i 8 (concatD [prt 8 exp1, doc (showString "!="), prt 9 exp2])
    AbsCPP.EAnd exp1 exp2 -> prPrec i 4 (concatD [prt 4 exp1, doc (showString "&&"), prt 5 exp2])
    AbsCPP.EOr exp1 exp2 -> prPrec i 3 (concatD [prt 3 exp1, doc (showString "||"), prt 4 exp2])
    AbsCPP.EAss exp1 exp2 -> prPrec i 2 (concatD [prt 3 exp1, doc (showString "="), prt 2 exp2])
    AbsCPP.EPlusAss exp1 exp2 -> prPrec i 2 (concatD [prt 3 exp1, doc (showString "+="), prt 2 exp2])
    AbsCPP.EMinusAss exp1 exp2 -> prPrec i 2 (concatD [prt 3 exp1, doc (showString "-="), prt 2 exp2])
    AbsCPP.Econditional exp1 exp2 exp3 -> prPrec i 2 (concatD [prt 3 exp1, doc (showString "?"), prt 2 exp2, doc (showString ":"), prt 2 exp3])
    AbsCPP.ETrow exp -> prPrec i 1 (concatD [doc (showString "throw"), prt 2 exp])
    AbsCPP.ETyped exp type_ -> prPrec i 16 (concatD [doc (showString "("), prt 0 exp, doc (showString ":"), prt 0 type_, doc (showString ")")])
  prtList _ [] = concatD []
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [String] where
  prt = prtList

instance Print AbsCPP.Idq where
  prt i e = case e of
    AbsCPP.EVector vectortype restvector -> prPrec i 0 (concatD [doc (showString "vector"), doc (showString "<"), prt 0 vectortype, doc (showString ">"), prt 0 restvector])
    AbsCPP.Idq id -> prPrec i 0 (concatD [prt 0 id])
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString "::"), prt 0 xs]

instance Print AbsCPP.VectorType where
  prt i e = case e of
    AbsCPP.VectorType1 type_ -> prPrec i 0 (concatD [prt 0 type_])
    AbsCPP.VectorType2 id -> prPrec i 0 (concatD [prt 0 id])

instance Print AbsCPP.RestVector where
  prt i e = case e of
    AbsCPP.EVectorVar ids -> prPrec i 0 (concatD [prt 0 ids])
    AbsCPP.EVectorNoVar -> prPrec i 0 (concatD [])

instance Print [AbsCPP.Idq] where
  prt = prtList

instance Print [AbsCPP.Exp] where
  prt = prtList

instance Print AbsCPP.Type where
  prt i e = case e of
    AbsCPP.Type_bool -> prPrec i 0 (concatD [doc (showString "bool")])
    AbsCPP.Type_int -> prPrec i 0 (concatD [doc (showString "int")])
    AbsCPP.Type_double -> prPrec i 0 (concatD [doc (showString "double")])
    AbsCPP.Type_void -> prPrec i 0 (concatD [doc (showString "void")])
    AbsCPP.Type_char -> prPrec i 0 (concatD [doc (showString "char")])

instance Print [AbsCPP.Id] where
  prt = prtList

